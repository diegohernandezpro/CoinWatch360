// ```import styled, {css} from 'styled-components';

// const colorHelper = ({color}) => css`
//     color: ${color};
// `

// const Text = styled.span`
//     ${colorHelper}
//     ${(props) => colorHelper(props)}
//     ${(props) =>
//       props.size &&
//       css`
//         font-size: ${props.size};
//       `}

//     font-size: ${(props) => props.size};
// `;
